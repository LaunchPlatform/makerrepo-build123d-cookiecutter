[project]
name = "makerrepo-build123d-cookiecutter"
version = "0.1.0"
description = "Cookiecutter template for Build123D repositories on MakerRepo.com"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "cookiecutter>=2.0",
]
